// Real-world use of Strings Code 3: Validate Password Length

let password = "barad";
if(password.length < 8) {
  console.log("Invalid: Your Password must be at least 8 characters long");
} else {
  console.log("Valid Password");
}
